import React from 'react'
import PropTypes from 'prop-types'

import './style.css'

const NavBar = (props) => {
  return (
    <section className="navbar">
      <div className="container">
        <div className="logo">Vouchernaut</div>
      </div>
    </section>
  )
}

NavBar.propTypes = {}

export default NavBar
