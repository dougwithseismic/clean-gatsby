import React from 'react';
import PropTypes from 'prop-types';

const SmallOfferBox = props => {
    return (
        <div className='small-offer-box'>
            
        </div>
    );
};

SmallOfferBox.propTypes = {
    
};

export default SmallOfferBox;